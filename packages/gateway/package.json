{
  "name": "@pino-agent/gateway",
  "version": "0.2.0-dev",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "test": "node --experimental-strip-types --test test/**/*.test.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.80.6",
    "@pino-agent/storage": "0.2.0-dev",
    "@pino-agent/transport": "0.2.0-dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
